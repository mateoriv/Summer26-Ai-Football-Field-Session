[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
# These tests exercise the pure (deterministic) helpers only -- no GPU, no
# video/model files, no Qt event loop. They should run in a few seconds.
